# Copy to .env.local (gitignored) and fill in real values before running
# the standalone manual scripts in this directory (smoke-test-prod.mjs,
# etc.). Run with: node --env-file=.env.local <script>.mjs
#
# Real Moodle console_admin account (kost_console_admin_role). Never commit
# real values — this file only documents the variable names.
CONSOLE_ADMIN_USER=
CONSOLE_ADMIN_PASS=
