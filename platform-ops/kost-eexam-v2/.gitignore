# deps
/node_modules

# next
/.next/
/out/
next-env.d.ts

# production data — jamais commité
/data/*.db
/data/*.db-shm
/data/*.db-wal
/data/backups/
/data/backup-log.jsonl

# env
.env
.env.local
.env*.local

# tests
/test-results/
/playwright-report/
/playwright/.cache/
# fixture E2E régénérée à chaque run (scripts/seed-email-demo.ts) — contient
# un jeton d'activation de TEST à usage unique, jamais un secret de prod.
/tests/e2e/.email-fixtures.json

# misc
.DS_Store
*.tsbuildinfo
.moodle-extracts/
.tier-a-extracts/
